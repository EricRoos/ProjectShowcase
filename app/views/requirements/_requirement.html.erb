<div id="<%= dom_id requirement %>">
  <div class="my-5">
    <%= requirement.story %>
  </div>

  <% if action_name != "show" %>
    <%= link_to 'Edit this requirement', edit_requirement_path(requirement), class: "text-sm rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
  <% end %>
</div>
